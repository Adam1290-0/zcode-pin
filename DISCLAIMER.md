# Disclaimer

本项目（ZCode Pin）以**学习与技术研究**为目的，通过本地补丁方式修改 ZCode 桌面客户端（`zcode.cjs` 与 `app.asar`），实现对话上下文的"钉住内容每轮注入"功能。

## 风险告知

1. **修改第三方软件**：本项目会向 ZCode 的 CLI 核心注入一行 `require` 并解包/重新打包其 `app.asar`。尽管补丁流程自带备份与还原脚本（`.pinbak`），操作失误仍可能导致 ZCode 无法启动或异常。
2. **数据外发**：pin 内容每轮都会随请求发送给你当前使用的模型服务及其上游链路（非仅本地），且每轮重发会增加 token 消耗。**请勿 pin 密钥、口令等机密内容**；涉及报价/合同的敏感内容请自行权衡。
3. **数据存储**：pin 内容与运行日志以明文存于本机用户目录（`~/.zcode/plugins/pin/`）。本地服务仅绑定 127.0.0.1 且带身份验证握手，但同权限的本机进程理论上可读取本地文件——这是本地注入类工具的固有边界。
4. **版本兼容**：ZCode 升级后补丁会失效，需要重新执行补丁脚本；不同版本间的内部结构可能变化，无法保证兼容。
5. **非官方**：本项目与 ZCode 官方无任何关联，仅为社区研究性质工具。

## 使用前提

- 仅用于你自己使用的客户端与你自己有权使用的 API 服务。
- 遵守目标 AI 服务的使用规则。

**使用即表示你已知悉并自行承担全部风险。**